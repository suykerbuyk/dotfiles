# Home-image content lives in the stow package home/.
home/.ssh/hosts/
home/.ssh/sockets/
home/.vimbackup/*
home/.local/bin/*
home/.config/zed/settings_backup.json
!home/.local/bin/fetch.all.bins.sh
!home/.local/bin/fetch.bins/
home/.local/share/*
home/.local/apps/*

# Repo-root artifacts: agent/vibe-palace state. Never tracked, never stowed
# (they sit outside the home/ package, so stow cannot see them). These live in
# the vibe-palace vault (Projects/dotfiles/), never in this repo.
/CLAUDE.md
/AGENTS.md
/commit.msg
/resume.md
/iterations.md
/tasks/
/.claude/
/.grok/
/.vibe-palace/
